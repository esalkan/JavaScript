/**
 * INITIALIZING VARIABLES WITH THE ASSIGNMENT OPERATOR
 * 
 * It is common the initialize a variable to an initial value in the same line as it is declared.
 */

var myVar = 0;

/**
 * Creates a new variable called "myVar" and assigns it an initial value of "0".
 */

// Define a variable "a" with "var" and initialize it a value of "9".
var a = 9;
console.log(a);